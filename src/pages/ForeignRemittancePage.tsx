import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/Card';

export default function ForeignRemittancePage() {
  return (
    <div className="page-enter space-y-8">
      <Card>
        <CardHeader><CardTitle>Foreign Remittance</CardTitle></CardHeader>
        <CardContent>
          <p className="text-text-secondary">Foreign Remittance content coming in Stage 5.</p>
        </CardContent>
      </Card>
    </div>
  );
}
