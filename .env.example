# Copy to ".env" and fill in the anon key from Supabase → Project Settings → API
# URL must be the project root only — do NOT add /rest/v1
VITE_SUPABASE_URL=https://fcuxncgafmtfmagtzouh.supabase.co
VITE_SUPABASE_ANON_KEY=
