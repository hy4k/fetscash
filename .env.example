# Copy to ".env" and fill in the anon key from Supabase → Project Settings → API
# URL must be the project root only — do NOT add /rest/v1
VITE_SUPABASE_URL=https://fcuxncgafmtfmagtzouh.supabase.co
VITE_SUPABASE_ANON_KEY=

# Optional: UUID used for categories, expenses, invoices, etc. (no auth — app uses anon key only).
# If unset, the app tries to read the first user_id from categories or expenses (requires RLS to allow it).
# VITE_WORKSPACE_USER_ID=
